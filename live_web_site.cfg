# Configuration for web site.

[buildout]
extends=live_base_conventional.cfg live_constants_site.cfg
site-type = web

[rebecca-supervisor-django]
port-prefix = ${buildout:port-prefix}1

[rebecca-nginx]
server-name = ${buildout:web-server-name}
